
var me = self()

if (active(me, "Dead")) {
  return
}

$move_target ??= {
  var origin = vec3(0, 0, 0)
  var radius = 150
  origin + random_circle_xz() * radius
}

if (nav_target(me) == $move_target && !active(me, "Traveling")) {
  $move_target = null
} else {
  nav_travel(me, $move_target)
}
