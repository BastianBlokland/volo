
if ($soundAsset && !$soundInstance) {
  var soundPos     = null // 2d sound.
  var soundGain    = 0.4
  var soundPitch   = 1.0
  var soundLooping = true
  $soundInstance   = sound_play($soundAsset, soundPos, soundGain, soundPitch, soundLooping)
  destroy_after($soundInstance, self())
}
