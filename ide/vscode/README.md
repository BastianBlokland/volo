# Vscode Volo extension

Vscode extension containing ide support for the Volo engine.

## Installation
* Copy the `volo` directory into your `<user home>/.vscode/extensions` directory.
* Reload vscode.
